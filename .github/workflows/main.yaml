name: "Main"
on:
  pull_request:
    paths-ignore:
      - "**/*.md"
  push:
    paths-ignore:
      - "**/*.md"
    branches:
      - "main"

permissions:
  contents: read
  id-token: write

jobs:
  build:
    name: "build"
    runs-on: "ubuntu-22.04"
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-go
        with:
          go-version-file: "go.mod"
          takumi-guard-bot-id: ${{ vars.TAKUMI_GUARD_BOT_ID }}
      - run: go mod tidy && make generate && make manifests && make check-uncommitted
      - run: make build
      - run: make lint
      - run: make test
