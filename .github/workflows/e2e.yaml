name: "e2e"
on:
  pull_request:
    paths-ignore:
      - "**/*.md"
      - "NOTICE"
      - "LICENSE"
  push:
    paths-ignore:
      - "**/*.md"
      - "NOTICE"
      - "LICENSE"
    branches:
      - "main"

env:
  # Github runner's extra disk is mounted on the /mnt directory.
  MINIKUBE_HOME: "/mnt/"

jobs:
  build:
    runs-on: "ubuntu-22.04"
    strategy:
      fail-fast: false
      matrix:
        label-filter:
          - full-backup
          - incremental-backup
          - misc
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"

      - name: df just before freeing up disk space
        run: |
          df -h

      - name: Free up disk space
        run: |
          df -h /
          sudo rm -rf /usr/share/dotnet /usr/local/.ghcup
          df -h /

      - name: Make /mnt accessible from `runner` user
        run: |
          sudo chown runner:runner /mnt
      - name: df just after freeing up disk space
        run: |
          df -h

      - uses: ./.github/actions/set-up-kvm-for-e2e-tests
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push Docker image with cache
        uses: docker/build-push-action@v6
        with:
          context: .
          tags: controller:latest
          load: true
          cache-from: type=gha,scope=${{ github.ref_name }}
          cache-to: type=gha,mode=max,scope=${{ github.ref_name }}

      - name: df before setup minikube
        run: |
          df -h

      - name: Setup minikube with kvm2 driver
        run: |
            make -C test/e2e launch-minikube

      - name: df after setup minikube
        run: |
          df -h
      - name: Load image into minikube
        run: |
            make -C test/e2e image-tar-load

      - name: df after loading image into minikube
        run: |
          df -h
