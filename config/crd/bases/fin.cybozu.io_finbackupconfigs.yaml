---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: finbackupconfigs.fin.cybozu.io
spec:
  group: fin.cybozu.io
  names:
    kind: FinBackupConfig
    listKind: FinBackupConfigList
    plural: finbackupconfigs
    singular: finbackupconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: FinBackupConfig is the Schema for the finbackupconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FinBackupConfigSpec defines the desired state of FinBackupConfig
            properties:
              node:
                type: string
              pvc:
                type: string
                x-kubernetes-validations:
                - message: spec.pvc is immutable
                  rule: self == oldSelf
              pvcNamespace:
                type: string
                x-kubernetes-validations:
                - message: spec.pvcNamespace is immutable
                  rule: self == oldSelf
              schedule:
                pattern: ^\s*([0-5]?[0-9])\s+(0?[0-9]|1[0-9]|2[0-3])\s+\*\s+\*\s+\*\s*$
                type: string
              suspend:
                default: false
                type: boolean
            required:
            - node
            - pvc
            - pvcNamespace
            - schedule
            type: object
          status:
            description: FinBackupConfigStatus defines the observed state of FinBackupConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
